# Quiz App Context

CIPT 备考应用的题库、导入与练习语言表，用于统一用户体验和题库质量相关概念。

## Language

**可入库题目**:
同时具备题干、至少两个选项、正确答案，且正确答案能匹配到选项的题目。
_Avoid_: 完整题目、有效题目

**不可用题目**:
缺少题干、缺少选项、缺少正确答案，或正确答案无法匹配选项的题目。
_Avoid_: 待复核题目、异常题目

**自动入库**:
程序将可入库题目直接写入题库的处理结果。
_Avoid_: 自动接纳、自动通过

**自动跳过**:
程序保留不可用题目的导入解析记录但不写入题库的处理结果。
_Avoid_: 自动删除、自动丢弃

**自动处理记录**:
一次导入任务中由程序自动入库或自动跳过的题目级处理明细。
_Avoid_: 自动处理统计、导入日志

**AI 解析**:
用户在练习、错题或题目查看场景中主动请求的辅导型解释，用于帮助理解正确答案和干扰项。
_Avoid_: 导入解析、参考解析、题目解析导入结果

## Relationships

- 一个**可入库题目**可以进入题库并用于练习。
- 一个**不可用题目**不应进入题库，也不应要求用户复核。
- **自动入库**产生正式题库题目。
- **自动跳过**只保留导入解析记录，不产生正式题库题目。
- 一条**自动处理记录**属于一个导入任务，并对应一道被自动处理的题目。
- 导入任务产生的解析内容不进入正式题目体验，正式题目只保留**AI 解析**。
- 答题结果、错题本和历史记录中的解析内容只表示**AI 解析**。
- 题目已有**AI 解析**时，用户再次请求 AI 解析只展示已有结果，不重新生成。

## Example dialogue

> **Dev:** “如果导入结果没有答案，但题干和选项看起来正常，要让用户复核吗？”
> **Domain expert:** “不要；它是**不可用题目**，不能用于练习，应自动跳过，并出现在**自动处理记录**里。”

## Flagged ambiguities

- “题目完整”已收敛为**可入库题目**：不仅要有题干和选项，也必须有可匹配到选项的正确答案。
