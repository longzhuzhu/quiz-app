# 后端 API 的公网地址（不要带尾部斜杠，也不要带 /api 后缀）
# 本地开发若留空，将走 vite.config.js 中的 /api 代理到 http://127.0.0.1:5003
# 生产 / v0.app / Vercel 部署时，请填写已部署的 FastAPI 后端地址
# 示例：
# VITE_API_BASE_URL=https://quiz-api.your-domain.com
VITE_API_BASE_URL=
